class User:
    def __init__(self, uid, name, password):
        self.uid = uid
        self.name = name
        self.pw = password
        #self.userData = ...................

    def changePassword(self, newPw, confirmPw)
        if newPw == confirmPw:
            self.pw = newPw

    
